# --- !Ups

CREATE TABLE IF NOT EXISTS "USER"
(
    ID INTEGER NOT NULL,
    USERNAME VARCHAR(30),
    PASSWORD VARCHAR(30),
    PRIMARY KEY(ID)
);

CREATE TABLE IF NOT EXISTS TRASH
(
    ID INTEGER NOT NULL,
    USER_ID INTEGER,
    VOLUME INTEGER,
    DUMP_DATE DATE,
    PRIMARY KEY(ID)
);

ALTER TABLE TRASH
    ADD    FOREIGN KEY (USER_ID)
    REFERENCES "USER"(ID)
    MATCH SIMPLE
;
    
# --- !Downs

DROP TABLE TRASH;
DROP TABLE "USER";